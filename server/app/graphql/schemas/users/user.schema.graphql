type Query {
  user(id: ID!): User
  users: [User]
}

type Mutation {
  createUser(
    name: String!
    birthDate: String!
    email: String!
    password: String!
    bodyWeight: Float!
    height: Float!
  ): User
  updateUser(id: ID!, name: String, email: String): User
}

type User {
  id: ID!
  name: String
  email: String
  birthDate: String
  numberOfWorkouts: Int
  bodyWeight: Float
  height: Float
  exercises: [Exercise]
  workouts: [Workout]
  workoutTemplates: [WorkoutTemplate]
}
